import React from 'react';
import { shallow } from 'enzyme';

describe('Row', () => {
  it('should run this test3', () => {
    expect(true).toBe(true);
  });
  it('should run this test4', () => {
    expect(true).toBe(true);
  });
});
