import React from 'react';
import { shallow } from 'enzyme';

describe('Row', () => {
  it('should run this test1', () => {
    expect(true).toBe(true);
  });
  it('should run this test2', () => {
    expect(true).toBe(true);
  });
});
